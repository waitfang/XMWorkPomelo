#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../sysstat/bin/sar-to-csv" "$@"
  ret=$?
else 
  node  "$basedir/../sysstat/bin/sar-to-csv" "$@"
  ret=$?
fi
exit $ret
